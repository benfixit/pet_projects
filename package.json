{
  "name": "pet_project",
  "npmClient": "yarn",
  "private": true,
  "packages": ["packages/*"],
  "useWorkspaces": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "run:weight": "cd packages/weight_tracker && yarn start"
  }
}