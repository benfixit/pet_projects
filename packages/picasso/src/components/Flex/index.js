import styled from 'styled-components';
import { flexbox } from 'styled-system';

const Flex = styled.div`
  display: flex;
  width: 100%;
  height: 100%;
  align-items: center;
  flex-wrap: wrap;
  ${flexbox}
`;

export default Flex;
